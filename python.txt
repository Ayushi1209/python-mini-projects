# Assuming $asgName is a variable containing the dynamic ASG name
$asgName = "your_dynamic_asg_name"

# Using the variable in the Azure CLI command
$resourceGroup = (az network asg show --name $asgName --query 'resourceGroup' -o tsv)

# Outputting the result
Write-Output $resourceGroup
