- name: Retrieve Resource ID for each ASG
  command: >
    az network asg show -n "{{ item }}"
    -g "{{ 'me2-infosec-rg' if cloudEnvironment in ['dev', 'test'] else 'mc1-infosec-rg' }}"
    --query 'id'
  register: result1
  when:
    - cloudEnvironment == 'prod'
    - resource_group == 'mc1-infosec-rg'
