---
- hosts: localhost
  vars:
    username: "{{ lookup('azure_keyvault_secret', 'serviceNowUser') }}"
    password: "{{ lookup('azure_keyvault_secret', 'servicenowPass') }}"
  tasks:
    - name: Run your script
      command: python your_script.py --username "{{ username }}" --password "{{ password }}"
