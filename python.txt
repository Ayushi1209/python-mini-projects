command: az network asg show -n "{{ item }}" -g "{{ 'me2-infosec-rg' if cloudEnvironment in ['dev', 'test'] else 'mc1-infosec-rg' }}" --query 'id'

command: >
  az network asg show -n "{{ item }}"
  -g "{{ 'me2-infosec-rg' if cloudEnvironment in ['dev', 'test'] else 'mc1-infosec-rg' }}"
  --query 'id'
