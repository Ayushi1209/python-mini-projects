
ASG will always be in the 
me2-infosec-rg or mc1-infosec-rg
me2 or dev/test mc1 for prod

 - name: Create a list from result.stdout_lines
      set_fact:
        stdout_lines_list: "{{ result.stdout_lines }}"

    # Optional: You can debug or print the list if needed
    - debug:
        var: stdout_lines_list

- name: Retrieve Resource ID
  command: az network asg show -n {{ asg_name }} -g {{ resource_group }} --query 'id'
  register: result1

- name: Clean the Resource ID
  set_fact:
    resource_id: "{{ result1.stdout_lines[0] | regex_replace('^\"|\"$', '') }}"

- name: Print resource id
  debug:
    var: resource_id
