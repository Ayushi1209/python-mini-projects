- name: Retrieve Resource ID for each ASG
  command: az network asg show -n "{{ item }}" -g {{ resource_group }} --query 'id'
  register: result1
  when:
    - cloudEnvironment == 'dev' or cloudEnvironment == 'test'
    - resource_group == 'me2-infosec-rg'

- name: Retrieve Resource ID for each ASG (prod)
  command: az network asg show -n "{{ item }}" -g mc1-infosec-rg --query 'id'
  register: result1
  when:
    - cloudEnvironment == 'prod'
    - resource_group == 'mc1-infosec-rg'
