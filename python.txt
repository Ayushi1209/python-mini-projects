import os

print("USERNAME_ENV_VARIABLE:", os.environ.get('USERNAME_ENV_VARIABLE'))
print("PASSWORD_ENV_VARIABLE:", os.environ.get('PASSWORD_ENV_VARIABLE'))


- name: Run your script with environment variables
  hosts: your_target_hosts
  tasks:
    - name: Set environment variables and run script
      command: /path/to/python your_script.py
      environment:
        USERNAME_ENV_VARIABLE: 'your_username'
        PASSWORD_ENV_VARIABLE: 'your_password'
