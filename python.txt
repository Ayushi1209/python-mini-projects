
---
- name: Get Azure ASG Resource ID by Name
  hosts: localhost
  tasks:
    - name: Get ASG Resource ID
      azure_rm_securitygroup_info:
        name: "{{ your_asg_name }}"
      register: asg_info

    - name: Display ASG Resource ID
      debug:
        var: asg_info.ansible_facts.id
